1.先看一下 jira link 这个任务，我们先统计一下re-features 这个仓库里面的组件用的接口用到memberslink 的地方，以组件为单位，记住只统计接口，以membersLink 为域名的页面链接不要算进去，不用打印出来，直接写入到一个表格里，我们第一步是一个草案，把每个组件的哪个位置用了什么接口写一下就行，放到一个markdown表格里
2.接下来去和 re-feaures 同级的 re-research-tools，re-listing-builder,re-listing-analyzer,re-control-center 四个仓库寻找有没有调用你列出来的组件


